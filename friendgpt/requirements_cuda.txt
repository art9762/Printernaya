# FriendGPT CUDA Training — зависимости для NVIDIA GPU
# Установка: pip install -r requirements_cuda.txt
#
# ВАЖНО: сначала установите PyTorch с CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

torch>=2.2.0
transformers>=4.44.0
datasets>=2.19.0
peft>=0.11.0
trl>=0.9.0
bitsandbytes>=0.43.0
accelerate>=0.30.0
sentencepiece>=0.2.0
protobuf>=4.25.0
scipy>=1.13.0
safetensors>=0.4.3
huggingface_hub>=0.23.0

# Опционально: Flash Attention 2 (для ещё большей скорости)
# pip install flash-attn --no-build-isolation
